import React from 'react'

const About = ({ innerRef }) => {
  return (
    <section id="about" ref={innerRef} className="min-h-screen flex items-center justify-center">
      about
    </section>
  )
}

export default About
